//문자열 길이 알아 내는방법

/*
문자열.length
str["length"]


문자열 붙이는 방법
예: 변수 str, str2   -str에는 Hello를 str2에는 world를

str.concat(str2);

출력 결과 : Helloworld

str3=str.concat(str2);
결과 값을 str3에 저장


str.concat(str2).concat("!");

식으로 문자열을 추가할 수있다. //변수 뿐 아니라 문자열도 가능.

"Hello".concat("world").concat("!");
이것도 동일한 출력으로 사용 가능

"hello".length

str+str2; 문자열 더하기 가능

"Hello"+" world";
문자+ 문자 가능
"Pi is "+3.14
문자열 + 숫자 가능

3.14 + " is Pi";
숫자 + 문자 가능

*/
